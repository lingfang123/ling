package com.yzf.util;


public class HttpAcceptResponseUtil {
	/*public <T> T getmessage(HttpServletRequest request,HttpServletResponse response) {
		//判断请求报文是否来自代维系统的ip地址  
		String ip = request.getRemoteHost(); 
		System.out.println("ip地址="+ip);
		
		try {
			//获取接收的报文
			BufferedReader reader=request.getReader();
			
			String line="";
			
			StringBuffer inputString = new StringBuffer();
			while ((line = reader.readLine()) != null) {
				inputString.append(line);
			}
			//jackJson    
			ObjectMapper o=new ObjectMapper();
			
			T t=o.readValue(inputString.toString(), T.class);
			System.out.println("接收的报文为= "+t);
		} catch (IOException e) {
			e.printStackTrace();
		}finally {
			//返回虔诚创投的参数
	        hf.setMerid(q.getMerid());
	        hf.setQcuname(q.getQcuname());
			try {
				ObjectMapper m = new ObjectMapper();
				String sendStr = m.writeValueAsString("返回内容");
				
				// 设置发送报文的格式  
				response.setContentType("text/json");
				response.setCharacterEncoding("UTF-8");
				
				PrintWriter out = response.getWriter();  
				out.println(sendStr);  
				out.flush();  
				out.close(); 	
			} catch (Exception e2) {
				e2.printStackTrace();
			}	
		}
	}*/
}
